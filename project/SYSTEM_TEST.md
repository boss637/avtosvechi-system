🧪 Запустить Комплексный Тест Системы

1. Назначение
Эта команда запускает полную, комплексную проверку работоспособности всех ключевых сервисов проекта "AUTOSHOP" с помощью скрипта healthcheck.sh.

Скрипт последовательно опрашивает `API, Shell Agent, Nginx, Базу данных, Redis` и другие компоненты, а затем формирует отчет о состоянии системы.

Используйте его, чтобы быстро убедиться, что все работает как надо после внесения изменений или перезапуска.

2. Команда для выполнения
Скопируйте команду ниже и вставьте ее в терминал (в корневой папке проекта `/home/oleg/autoshop`). Для выполнения требуются права sudo.
```bash
cd ~/autoshop && sudo ./healthcheck.sh
```
