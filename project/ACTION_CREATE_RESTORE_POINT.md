## 🚀 Создать Точку Восстановления Системы
## 1. Назначение
**Эта команда является главным инструментом сохранения состояния проекта. Она выполняет два ключевых действия одновременно:**

**Создает полный локальный бэкап (дамп базы данных + архив всего кода) на сервере.**

**Отправляет все изменения кода в ваш репозиторий на GitHub.**

**Используйте ее после каждого значимого изменения, чтобы иметь надежную точку для отката.**

## 2. Команда для выполнения
**Скопируйте команду ниже, вставьте ее в терминал (в корневой папке проекта `/home/oleg/autoshop`) и замените текст в кавычках на понятное описание проделанной работы.**

```bash
cd ~/autoshop && ./create_restore_point.sh "Система восстановлена .2026"
```

**Пример использования**
```bash
cd ~/autoshop && ./create_restore_point.sh "Система восстановлена 10.01.2026"
```

## Проверка на Сервере
**Цель: Увидеть список файлов в папке backups, чтобы убедиться, что дамп БД и архив кода лежат на месте.**

**Ваше действие: Скопируйте и выполните эту простую команду в терминале.*

```bash
ls -lh ~/autoshop/backups/
```

**Что вы должны увидеть:**
**Вывод будет примерно таким (точные размеры и даты будут ваши):**
```bash
-rw-r--r-- 1 root root  12K Jan 10 06:09 code_snapshot_2026-01-10_06-09-37.tar.gz
-rw-r--r-- 1 oleg oleg 1.2K Jan 10 06:09 db_dump_2026-01-10_06-09-37.sql.gz
```
**Это покажет, что оба файла (.tar.gz и .sql.gz) физически существуют на вашем сервере.**

