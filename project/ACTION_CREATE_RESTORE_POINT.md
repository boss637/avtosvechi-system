## 🚀 Создать Точку Восстановления Системы

## 1. Назначение

Эта команда — главный инструмент сохранения **полного состояния проекта**. Она создает **структурированную точку восстановления**, которая включает:

*   **Дамп базы данных** в формате SQL
*   **Архив всего исходного кода** проекта
*   **Мета-информацию** о состоянии системы

Каждая точка сохраняется в отдельной директории с уникальным именем, содержащим дату, описание и хеш Git-коммита. Используйте эту команду **перед любыми значимыми изменениями** в системе.

## 2. Обновленная структура хранения (ВАЖНО!)

Система была улучшена. Теперь **каждая точка восстановления — это отдельная папка** в директории `~/autoshop/backups/`, а не отдельные файлы.

**Пример структуры `backups/`:**
```
backups/
├── latest -> Идеальная_точка_восстановления_22.01.2026_коммит_d701aac
├── CURRENT_STABLE.txt
├── 2026-01-22_Идеальная_точка_коммит_d701aac/
│   ├── db_dump.sql.gz
│   ├── code_snapshot.tar.gz
│   └── README.md
└── ... (другие точки)
```

*   **`latest`** — символическая ссылка на **последнюю созданную** точку восстановления.
*   **`CURRENT_STABLE.txt`** — текстовый файл, указывающий на **проверенную, стабильную** точку для восстановления.

## 3. Команда для выполнения

Перейдите в корневую директорию проекта и выполните команду, заменив `"Ваше_описание"` на понятное описание изменений (например, `"Добавлен_новый_агент_мониторинга"`).

```bash
cd ~/autoshop && ./create_restore_point.sh "Ваше_описание"
```

**Пример использования с рекомендуемым форматом:**

```bash
cd ~/autoshop && ./create_restore_point.sh "Идеальная_точка_восстановления_$(date +%d.%m.%Y)_коммит_$(git rev-parse --short HEAD)"
```

## 4. Проверка на Сервере

**Цель:** Убедиться, что точка восстановления создана корректно и увидеть её в списке.

Выполните команду:

```bash
ls -la ~/autoshop/backups/ | grep -E "latest|^d.*$(date +%Y-%m)" | head -10
```

Что вы должны увидеть:

1. Символическую ссылку `latest`, указывающую на свежую директорию.
2. Новую директорию с именем, начинающимся на сегодняшнюю дату (например, `2026-01-23_...`).

Для детального просмотра содержимого конкретной точки используйте:

```bash
ls -lh ~/autoshop/backups/latest/
```

## 5. Использование точек восстановления

Для быстрого доступа в скриптах всегда используйте ссылку `latest`:

```bash
# Пример восстановления БД из последней точки
cat ~/autoshop/backups/latest/db_dump.sql.gz | gunzip | docker-compose exec -T postgres psql -U autoshop -d autoshop_db
```

Для контролируемого восстановления проверьте файл `CURRENT_STABLE.txt`:

```bash
# Узнать, какая точка помечена как стабильная
STABLE_POINT=$(cat ~/autoshop/backups/CURRENT_STABLE.txt | head -1)
echo "Стабильная точка для восстановления: $STABLE_POINT"
```

📅 **Последнее обновление документации:** 23.01.2026
🔗 Актуальная стабильная точка: Идеальная_точка_восстановления_22.01.2026_коммит_d701aac

## 6. Устранение проблем (Troubleshooting)

### Проблема: "ОШИБКА: Не удалось создать архив кода"
**Решение:**
```bash
# Исправление прав доступа на __pycache__ директории
sudo find ~/autoshop -name "__pycache__" -type d -exec chown -R oleg:oleg {} \; 2>/dev/null
```

### Проблема: Не хватает места на диске
**Решение:**
```bash
# Удаление старых точек восстановления (старше 30 дней)
find ~/autoshop/backups -type d -name "2026-*" -mtime +30 -exec rm -rf {} \;
```

### Проблема: Контейнер PostgreSQL не запущен
**Решение:**
```bash
docker-compose ps postgres
docker-compose up -d postgres
```

## 7. Диагностические команды

## Быстрая проверка системы:
```bash
cd ~/autoshop && ./create_restore_point.sh "Системная_проверка_$(date +%Y%m%d_%H%M%S)"

```

## Проверка существующих точек:
```bash
ls -la ~/autoshop/backups/latest/
gunzip -t ~/autoshop/backups/latest/db_dump.sql.gz && echo "✅ Дамп БД корректен"
tar -tzf ~/autoshop/backups/latest/code_snapshot.tar.gz >/dev/null && echo "✅ Архив кода корректен"
```

## Типичные проблемы и решения:
1. **"ОШИБКА: Не удалось создать архив кода"** - исправьте права: `sudo chown -R oleg:oleg ~/autoshop/__pycache__`
2. **Не хватает места** - удалите старые точки: `find ~/autoshop/backups -type d -name "2026-*" -mtime +30 -exec rm -rf {} \;`
3. **PostgreSQL не запущен** - запустите: `docker-compose up -d postgres`


## 8. Результаты тестирования системы (24.01.2026)

### ✅ Система полностью протестирована и работает:

1. **Создание точек восстановления:**
   - Дамп БД: корректный gzip архив
   - Архив кода: корректный tar.gz архив
   - Автоматическое обновление ссылки 

2. **Восстановление БД из точки:**
   - Восстановлено 14 таблиц в новой БД
   - Созданы все индексы и constraints
   - Команда восстановления работает корректно

## 8. Результаты тестирования системы (24.01.2026)

### ✅ Система полностью протестирована и работает:

1. **Создание точек восстановления:**
   - Дамп БД: корректный gzip архив
   - Архив кода: корректный tar.gz архив
   - Автоматическое обновление ссылки latest

2. **Восстановление БД из точки:**
   - Восстановлено 14 таблиц в новой БД
   - Созданы все индексы и constraints
   - Команда восстановления работает корректно

3. **Важные замечания:**
   - Правильное имя БД: autoshop_db (не autoshop)
   - DROP DATABASE и CREATE DATABASE нужно выполнять в отдельных подключениях
   - Для тестирования создавайте новую БД, а не используйте существующую
