#🧹 Руководство по автоматической очистке бэкапов

**Статус:** Актуальный  
**Дата:** 14 января 2026 г.  
**Связанные скрипты:** [`create_restore_point.sh`](../create_restore_point.sh), [`clean_old_backups.sh`](../clean_old_backups.sh)

**Это руководство описывает систему структурированных точек восстановления**.

### 🎯 Назначение системы

Данная система обеспечивает **структурированное хранение точек восстановления** и их **автоматическую ротацию** по принципу **"хранить N последних версий"**. Она заменяет хаотичное скопление отдельных файлов в папке `backups/` на упорядоченную, легко управляемую структуру.

### 📁 Новая структура хранения

Каждая точка восстановления создается в **отдельной именованной папке** по шаблону:
```bash
backups/ГГГГ-ММ-ДД_Краткое_описание/
├── db_dump.sql.gz # Дамп базы данных PostgreSQL
├── code_snapshot.tar.gz # Архив исходного кода проекта
└── README.md # Описание точки восстановления
```
```bash
**Символическая ссылка** `backups/latest` всегда указывает на последнюю созданную точку.

## 🛠️ Скрипт создания точки восстановления: `create_restore_point.sh`

### Назначение
Создает полную точку восстановления проекта, включая:
*   Дамп базы данных PostgreSQL
*   Архив исходного кода
*   Файл с описанием

### Использование
```bash
cd ~/avtosvechi-system
./create_restore_point.sh
```
### Скрипт запросит текстовое описание точки (например, `добавлен_новый_модуль`).

#### Что делает скрипт
1. **Создает папку с именем по шаблону `2026-01-14_добавлен_новый_модуль`**.

2. **Экспортирует базу данных в `db_dump.sql.gz`**.

3. **Архивирует код проекта в `code_snapshot.tar.gz` (исключая папки `backups/`, `.git/`, `postgres/data`)**.

4. **Создает README.md с описанием точки**.

5. **Обновляет символическую ссылку `latest`**.

###Конфигурация (при необходимости)

**Параметры подключения к БД заданы в начале скрипта**:
```bash
DB_NAME="autoshop_db"
DB_CONTAINER="autoshop_db"
DB_USER="autoshop"
```
##🗑️ Скрипт очистки: `clean_old_backups.sh`

**Назначение**
Автоматически удаляет старые папки-точки восстановления, оставляя только указанное количество самых свежих.

**Использование**
```bash
cd ~/avtosvechi-system
# Интерактивный режим (оставить 5 папок, с подтверждением)
./clean_old_backups.sh

# Автоматический режим (оставить 10 папок без подтверждения)
./clean_old_backups.sh --keep 10 --force

# Показать справку
./clean_old_backups.sh --help
```
### Принцип работы

1. **Находит все папки по шаблону `20[0-9][0-9]-[0-9][0-9]-[0-9][0-9]_*` в `backups/`**.

2. **Сортирует их по имени (что соответствует дате создания)**.

3. **Определяет, какие удалить: все, кроме N последних (по умолчанию N=5)**.

4. **Запрашивает подтверждение (если не указан `--force`)**.

5. **Удаляет старые папки вместе со всем их содержимым**.

### Конфигурация

Основной параметр задается в скрипте:
```bash
KEEP_COUNT=5  # Сколько последних папок оставлять
```
Может быть переопределен аргументом --keep N.

### ⚙️ Рекомендации по внедрению

**1. Автоматизация через `cron`**

Для регулярной очистки добавьте в `crontab`:
```bash
# Каждое воскресенье в 03:00 очищать бэкапы, оставляя 5 последних
0 3 * * 0 cd /home/oleg/avtosvechi-system && ./clean_old_backups.sh --force

# Ежедневно в 06:00 очищать, оставляя 10 последних (более мягкая политика)
0 6 * * * cd /home/oleg/avtosvechi-system && ./clean_old_backups.sh --keep 10 --force
```

**2. Создание точки восстановления после значимых изменений**

```bash
cd ~/avtosvechi-system
./create_restore_point.sh
# Ввести описание: "релиз_версии_2_1"
```

**3. Восстановление из точки**

```bash
# 1. Перейти в папку точки восстановления
cd ~/avtosvechi-system/backups/2026-01-14_релиз_версии_2_1

# 2. Восстановить код
tar -xzf code_snapshot.tar.gz -C /целевой/путь

# 3. Восстановить базу данных
gunzip -c db_dump.sql.gz | docker exec -i autoshop_db psql -U autoshop autoshop_db
```

## 🔍 Мониторинг и отладка

**Проверка состояния**
```bash
# Просмотр содержимого backups
ls -la ~/avtosvechi-system/backups/

# Проверка, куда ведет ссылка latest
ls -l ~/avtosvechi-system/backups/latest

# Проверка размера бэкапов
du -sh ~/avtosvechi-system/backups/*/
```

**Логирование**

Оба скрипта выводят подробные сообщения о своих действиях в стандартный вывод. Для логирования в файл перенаправьте вывод:
```bash
./clean_old_backups.sh --force >> /var/log/backup_clean.log 2>&1
```

## ⚠️ Важные замечания
1. Безопасность удаления: Скрипт `clean_old_backups.sh` использует `rm -rf`. Убедитесь, что: 
* Вы правильно указали `BACKUP_ROOT`.
* Папки соответствуют шаблону даты.

2. Связь `latest`: Ссылка `latest` обновляется только при создании новой точки. После удаления самой свежей папки она может стать "битой" до следующего запуска `create_restore_point.sh`.

3. Минимальное количество: Рекомендуется хранить не менее 3-5 точек для возможности отката.

### 📈 Эволюция системы

**Данная система была внедрена 14.01.2026 для замены старой схемы, где в `backups/` накапливались отдельные файлы (`*_2026-01-10_*.tar.gz`)**. **Все исторические файлы были удалены, остались только структурированные папки**.
___________________________________________________________________________________________________________________________

*Это руководство является частью документации проекта Avtosvechi System. Все скрипты находятся в корневой директории проекта (`~/avtosvechi-system/`)*.
```bash

**Ваши действия:**
1.  Откройте файл на GitHub: https://github.com/boss637/avtosvechi-system/blob/master/project/CLEAN_BACKUPS_GUIDE.md
2.  Нажмите кнопку **✏️ Edit this file** (карандаш).
3.  **Удалите** всё текущее содержимое.
4.  **Вставьте** текст, приведённый выше.
5.  Прокрутите вниз, введите описание коммита (например, "Полное наполнение руководства CLEAN_BACKUPS_GUIDE.md") и нажмите **Commit changes**.

Текст готов и отформатирован для удобного чтения на GitHub.
```
