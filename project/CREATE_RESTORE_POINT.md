# 🛡️ ЗАДАЧА «НЕПОТОПЛЯЕМОСТЬ»: Создание точки восстановления

**Цель:** Создать мгновенный, полный слепок системы (код + данные) для гарантированного отката при любой нештатной ситуации.

## 🎯 1. Суть процесса (двойное действие)

Основной скрипт `create_restore_point.sh` выполняет два критически важных действия **одновременно**:

| Действие | Что создает | Где хранится | Для чего нужно |
|----------|-------------|--------------|----------------|
| **1. Локальный бэкап** | 1. Дамп БД PostgreSQL (`*.sql.gz`) <br> 2. Архив всего кода (`*.tar.gz`) | Папка `~/autoshop/backups/` на сервере | **Аварийное восстановление** при сбое железа, ОС или удаления данных. |
| **2. Снимок кода в Git** | Точка истории (коммит) в репозитории | Ветка `master` на GitHub | **Безопасный откат** кода при внесении ошибочных изменений. |

**Итог:** После запуска команды вы получаете **полную гарантию** — состояние системы на этот момент можно восстановить в любой будущий момент.

## 🚀 2. Команда для создания точки восстановления

Перейдите в корневую директорию проекта и выполните команду, **обязательно указав в кавычках понятное описание**:

```bash
cd ~/autoshop
./create_restore_point.sh "Ваше понятное описание проделанной работы"
```

### 📌 Примеры использования (конкретные)

```bash
# После завершения крупной задачи или фикса
./create_restore_point.sh "Реализован API-метод поиска по VIN. Задача #123"

# Ежедневный бэкап в конце рабочего дня
./create_restore_point.sh "Daily backup: $(date +%Y-%m-%d)"

# После критического исправления в продакшене
./create_restore_point.sh "Hotfix: исправлена утечка памяти в обработчике заказов"
```

## 📁 3. Где лежат файлы (памятка администратора)

| Тип файлов | Расположение | Формат имен файлов |
|------------|--------------|---------------------|
| **Дампы базы данных** | `~/autoshop/backups/` | `db_dump_ГГГГ-ММ-ДД_ЧЧ-ММ-СС.sql.gz` |
| **Архивы кода** | `~/autoshop/backups/` | `code_snapshot_ГГГГ-ММ-ДД_ЧЧ-ММ-СС.tar.gz` |
| **История версий (коммиты)** | Ветка `master` репозитория `avtosvechi-system` на GitHub | Хэш коммита (например, `a1b2c3d`) |

### ✅ Быстрая проверка после создания точки

Чтобы убедиться, что файлы созданы, выполните:

```bash
ls -lh ~/autoshop/backups/
```

## 🔄 4. Как восстановиться: сценарии для администратора

### Сценарий А: Откат ошибочных изменений в коде (без удаления данных)

**Ситуация:** Вы внесли изменения в код, которые привели к ошибкам, и хотите вернуться к рабочей версии.

**Рекомендуемый способ (безопасный, сохраняет историю):**

```bash
# 1. Определите, сколько последних коммитов нужно отменить (N).
# 2. Создайте новые коммиты, отменяющие старые:
git revert HEAD~N..HEAD
# 3. Отправьте изменения:
git push origin master
```

**Аварийный способ (использовать с крайней осторожностью):**

```bash
# 1. Найдите хэш целевого коммита в истории (`git log --oneline`).
# 2. Откатите локальный репозиторий:
git reset --hard <хэш_коммита>
# 3. ПРИНУДИТЕЛЬНО синхронизируйте удаленный репозиторий (ПЕРЕЗАПИШЕТ ИСТОРИЮ):
git push origin master --force
```

⚠️ **КРИТИЧЕСКОЕ ПРЕДУПРЕЖДЕНИЕ:** Команда `git push --force` безвозвратно удалит все коммиты, сделанные после указанного хэша, из истории на GitHub. Используйте её, только если вы абсолютно уверены, что эти коммиты больше не нужны и вы работаете в одиночку.

### Сценарий Б: Полное аварийное восстановление (после катастрофы)

**Ситуация:** Сервер полностью вышел из строя. У вас есть новая чистая машина и файлы бэкапов.

```bash
# 1. Клонируйте репозиторий с кодом:
git clone https://github.com/boss637/avtosvechi-system.git ~/autoshop
cd ~/autoshop

# 2. Поместите самый свежий файл дампа БД (*.sql.gz) в папку backups/.

# 3. Восстановите базу данных (ПОДСТАВЬТЕ АКТУАЛЬНОЕ ИМЯ ФАЙЛА):
LATEST_DB=$(ls -t ~/autoshop/backups/db_dump_*.sql.gz | head -1)
gunzip -c "\$LATEST_DB" | docker compose exec -T postgres psql -U autoshop -d autoshop_db

# 4. Запустите всю систему:
docker compose up -d --build
```

## 🏁 Итог

Используйте команду `./create_restore_point.sh` после каждого значимого изменения. Это ваша главная страховка, реализующая принцип **«Несокрушимого Фундамента»**.
