## 🚀 Создать Точку Восстановления Системы

## 1. Суть — Простыми Словами
Эта команда — мгновенная защита вашего проекта от любых сбоев. Это как Time Machine, но для сервера и GitHub одновременно.

Ситуация	Без этой команды	С этой командой
Баг в коде	2 часа ручного поиска и отката	10 секунд (git reset)
Сервер "сгорел"	3 дня на восстановление с нуля	5 минут (восстановление из бэкапа)
Ежедневная рутина	15 минут ручного создания бэкапов	15 секунд (запуск одного скрипта)
 
## 2. Что делает скрипт `create_restore_point.sh`
**При запуске он выполняет три ключевых действия:**

**1. Создает дамп базы данных PostgreSQL.**

**2. Создает полный архив всего кода проекта.**

**3. Делает коммит и пуш всех изменений кода в репозиторий на GitHub.**

**Используйте эту команду после каждого значимого изменения, чтобы иметь надежную точку для отката.**

**3. Команда для выполнения**
Скопируйте команду ниже, вставьте ее в терминал (в корневой папке проекта `/home/oleg/autoshop`) и замените текст в кавычках на понятное описание проделанной работы.

```bash
./create_restore_point.sh "Ваше понятное описание проделанной работы"
```
**Пример использования**
```bash
# После завершения крупной задачи
./create_restore_point.sh "Полностью восстановлена работоспособность системы"

# Ежедневный бэкап в конце дня
./create_restore_point.sh "Daily backup"
```

## 4. Где Лежат Файлы
**Локальные бэкапы (дампы БД и архивы кода):**

**Расположение:** `/home/oleg/autoshop/backups/`

**Формат имен:**

`db_dump_ГГГГ-ММ-ДД_ЧЧ-ММ-СС.sql.gz`

`code_snapshot_ГГГГ-ММ-ДД_ЧЧ-ММ-СС.tar.gz`

**История версий кода:**

**Расположение: В вашем репозитории на GitHub, в ветке** `main`.

## 5. Как Восстановиться (ВАЖНО)

**Сценарий А: Откат Кода к Предыдущей Версии**
**Если вы внесли неудачные изменения в код и хотите их отменить.**

**Посмотрите историю коммитов: `git log`**

**Найдите хэш нужного коммита (например, `a1b2c3d`).**

**Откатите код к этому состоянию: `git reset --hard a1b2c3d`**

**Отправьте изменения на сервер (если нужно): `git push origin main --force`**

## Сценарий Б: Полное Аварийное Восстановление (после катастрофы)
**Если сервер "умер" и у вас есть только чистая машина.**

**Клонируйте репозиторий с кодом: `git clone <ваш_git_url>`**

**Найдите и скопируйте на сервер последний `.sql.gz` дамп базы данных из ваших внешних хранилищ.**

**3. Восстановите базу данных:**

```bash
# Распаковываем дамп
gunzip -c backups/db_dump_YYYY-MM-DD_HH-MM-SS.sql.gz > temp_dump.sql
# Запускаем контейнер с БД
docker compose up -d postgres
# "Заливаем" дамп в контейнер
cat temp_dump.sql | docker compose exec -T postgres psql -U autoshop -d autoshop_db
# Удаляем временный файл
rm temp_dump.sql
```

**Запустите всю систему: `docker compose up -d --build`**
**--- КОНЕЦ БЛОКА ДЛЯ `CREATE_RESTORE_POINT.md` ---**

**Результат:
Теперь ваша страница `CREATE_RESTORE_POINT.md` будет наглядной, как у Perplexity, и функциональной, как моя версия. Она будет содержать и "почему", и "что делать".**
