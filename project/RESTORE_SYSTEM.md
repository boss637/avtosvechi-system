# 🛡️ НЕСОКРУШИМЫЙ ФУНДАМЕНТ
> Система восстановления AUTOSHOP v5.0 (100 % покрытие)

## 🎯 ФИЛОСОФИЯ (2 уровня защиты)

| Уровень | Что защищает | Где хранится | GitHub |
|:---|:---|:---|:---|
| 🔴 **Аварийное** | Дампы БД + тома Docker | `~/backups/` + USB + GDrive | **НИКОГДА** |
| 🟢 **Версии кода** | Логика API и тесты | Ветка `main` репозитория | ✅ **ВСЕГДА** |

**Текущее состояние:**
*   **БД (21k SKU)**: 🟢 LIVE `db_dump_20260109_1030.sql.gz`
*   **Код (API+тесты)**: 🟢 `main` `a1b2c3d` (5 мин назад)
*   **Volumes**: 🟢 UP `autoshop_postgres_data`
*   **Мониторинг**: 🟢 100% `Grafana:3000`

---

## Философия

Эта система создана для обеспечения двух уровней защиты проекта:

1.  **Аварийное восстановление (локально):** Полные «слепки» системы (дамп БД + архив кода) на случай сбоя на сервере. Эти файлы **никогда не должны попадать в Git**.
2.  **Контроль версий кода (GitHub):** История изменений кода, позволяющая откатиться к любой предыдущей версии именно логики приложения.

**Ключевой инструмент:** `create_restore_point.sh`.
Это единый скрипт, который является «сердцем» системы. При запуске он выполняет три действия:
1.  Создаёт дамп базы данных PostgreSQL.
2.  Создаёт полный архив всего кода проекта.
3.  Выполняет коммит и пуш всех изменений кода в репозиторий на GitHub.

---

## Как создать точку восстановления

В терминале, в корневой папке проекта (`/home/oleg/autoshop`), выполните команду, **обязательно указав в кавычках понятное описание изменений:**

```bash
cd ~/autoshop && if [ -d ".git" ]; then ./create_restore_point.sh "Описание проделанной работы"; else echo "❌ Ошибка: Не найден Git репозиторий. Запустите из директории ~/autoshop"; fi
```

## Пример:

```bash
./create_restore_point.sh "Исправлена логика в API и добавлены новые тесты"
```

## 4. Где Лежат Файлы Локальные бэкапы (дампы БД и архивы кода):

Расположение: /home/oleg/autoshop/backups/

Формат имен:

db_dump_ГГГГ-ММ-ДД_ЧЧ-ММ-СС.sql.gz

code_snapshot_ГГГГ-ММ-ДД_ЧЧ-ММ-СС.tar.gz

## История версий кода:

Расположение: В вашем репозитории на GitHub, в ветке main.

## Как восстановить сценарий А: 
откат кода к предыдущей версии Если вы внесли неудачные изменения в код и хотите их отменить.
Посмотрите историю коммитов: 
```bash
cd ~/autoshop && git log
```

Найдите хэш нужного коммита (например, a1b2c3d).
Откатите код до этого состояния: 
```bash
git reset --hard a1b2c3d
```

Отправьте изменения на сервер (если нужно): 
```bash
git push origin main --force
```
## Сценарий Б: полное аварийное восстановление (после катастрофы) Если сервер «умер» и у вас есть только чистая машина.

Клонируйте репозиторий с кодом: 
```bash
git clone <ваш_git_url>
```

Найдите и скопируйте на сервер последний дамп базы данных .sql.gz из внешних хранилищ.

## Восстановите базу данных:

```bash
## Распаковываем дамп
gunzip -c backups/db_dump_ГГГГ-ММ-ДД_ЧЧ-ММ-СС.sql.gz > temp_dump.sql
```

## Запускаем контейнер с БД
```bash
docker compose up -d postgres
```

## Команда восстановления БД
```bash
cat temp_dump.sql | docker compose exec -T postgres psql -U autoshop -d autoshop_db
```
## Удаляем временный файл
rm temp_dump.sql Запустите всю систему: 
```bash
docker compose up -d --build 
```
## Финальная проверка
```bash
curl http://192.168.1.100:8000/health  # ✅ healthy
/search NGK BKR6E                      # ✅ 21k SKU готовы
```

## 📋 Полный скрипт автоматической проверки после восстановления
Перед выполнением: Этот скрипт выполняет все 4 этапа проверки системы после восстановления из точки восстановления. Выполняйте его ПОСЛЕ того, как команда `restore_system.sh` завершится успешно.

```bash
cd ~/autoshop && echo "==========================================================" && echo "🚀 ПОЛНАЯ ПРОЦЕДУРА ПРОВЕРКИ ПОСЛЕ ВОССТАНОВЛЕНИЯ СИСТЕМЫ" && echo "==========================================================" && echo "" && echo "📅 Дата и время начала: $(date)" && echo "🖥️  Хост: $(hostname)" && echo "📁 Рабочая директория: $(pwd)" && echo "" && echo "==================================================================" && echo "ЭТАП 1/4: НЕМЕДЛЕННАЯ ПРОВЕРКА (БАЗОВАЯ ФУНКЦИОНАЛЬНОСТЬ)" && echo "==================================================================" && echo "" && echo "1. 🔍 Проверка Docker и контейнеров:" && if command -v docker &> /dev/null; then echo "   ✅ Docker установлен: $(docker --version | head -1)" && echo "   🐳 Статус контейнеров:" && docker ps --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}" 2>/dev/null | head -10 || echo "     Нет запущенных контейнеров"; else echo "   ❌ Docker не установлен!"; fi && echo "" && echo "2. 🔌 Проверка основных портов системы Триады:" && declare -A PORTS=( ["Prometheus"]=9091 ["Executor API"]=8000 ["Solver API"]=8080 ["Redis"]=6379 ["PostgreSQL"]=5432 ["Grafana"]=3000 ) && for service in "${!PORTS[@]}"; do port=${PORTS[$service]} && echo -n "   $service (порт $port): " && if timeout 1 bash -c "echo > /dev/tcp/localhost/$port" 2>/dev/null; then echo "✅ открыт" elif docker ps | grep -q ":${port}->"; then echo "🔄 контейнер запущен, но порт закрыт" else echo "❌ закрыт/недоступен"; fi; done && echo "" && echo "3. 📊 Проверка ключевых процессов (через Docker):" && for service in postgres redis; do container_id=$(docker ps -q -f name=$service) && if [ -n "$container_id" ]; then echo "   $service: ✅ работает (контейнер: ${container_id:0:12})" else echo "   $service: ❌ не запущен"; fi; done && echo "" && echo "==================================================================" && echo "ЭТАП 2/4: ФУНКЦИОНАЛЬНОЕ ТЕСТИРОВАНИЕ СЕРВИСОВ" && echo "==================================================================" && echo "" && echo "4. 📈 Тестирование Prometheus (метрики):" && PROM_RESPONSE=$(timeout 5 curl -s http://localhost:9091/metrics 2>/dev/null | head -3) && if [ -n "$PROM_RESPONSE" ]; then echo "   ✅ Prometheus отвечает:" && echo "   Первые метрики: $(echo "$PROM_RESPONSE" | tr '\n' ' ' | cut -c1-80)..." else echo "   ❌ Prometheus не отвечает на порту 9091"; fi && echo "" && echo "5. 🤖 Тестирование Executor API (health check):" && EXEC_RESPONSE=$(timeout 5 curl -s http://localhost:8000/health 2>/dev/null) && if [ -n "$EXEC_RESPONSE" ]; then echo "   ✅ Executor API отвечает: $EXEC_RESPONSE" else echo "   ❌ Executor API не отвечает на порту 8000"; fi && echo "" && echo "6. 🗄️ Тестирование Redis (ping):" && REDIS_CONTAINER=$(docker ps -q -f name=redis) && if [ -n "$REDIS_CONTAINER" ]; then REDIS_PING=$(timeout 3 docker exec "$REDIS_CONTAINER" redis-cli ping 2>/dev/null) && if [ "$REDIS_PING" = "PONG" ]; then echo "   ✅ Redis отвечает: PONG" && echo "   Проверка очередей Redis:" && docker exec "$REDIS_CONTAINER" redis-cli keys "executor:*" 2>/dev/null | wc -l | xargs echo "     Найдено очередей:" else echo "   ❌ Redis не отвечает внутри контейнера"; fi else echo "   ❌ Контейнер Redis не найден"; fi && echo "" && echo "7. 💾 Тестирование базы данных PostgreSQL:" && PG_CONTAINER=$(docker ps -q -f name=postgres) && if [ -n "$PG_CONTAINER" ]; then DB_CHECK=$(timeout 5 docker exec "$PG_CONTAINER" psql -U autoshop -d autoshop -c "SELECT version();" 2>/dev/null | head -3) && if echo "$DB_CHECK" | grep -q "PostgreSQL"; then echo "   ✅ База данных работает:" && echo "   $(echo "$DB_CHECK" | grep "PostgreSQL" | head -1)" && echo "   Проверка миграций:" && docker exec "$PG_CONTAINER" psql -U autoshop -d autoshop -c "SELECT COUNT(*) FROM alembic_version;" 2>/dev/null || echo "     Не удалось проверить миграции" else echo "   ❌ Ошибка подключения к БД"; fi else echo "   ❌ Контейнер PostgreSQL не найден"; fi && echo "" && echo "==================================================================" && echo "ЭТАП 3/4: ПРОВЕРКА ДАННЫХ И КОНФИГУРАЦИЙ" && echo "==================================================================" && echo "" && echo "8. ⚙️ Проверка конфигурационных файлов:" && CONFIG_FILES=("docker-compose.yml" ".env" "docker-compose.dev.yml") && for config in "${CONFIG_FILES[@]}"; do if [ -f "$config" ]; then echo "   ✅ $config: существует ($(wc -l < "$config") строк, $(du -h "$config" | cut -f1))" else echo "   ⚠️  $config: отсутствует"; fi; done && echo "" && echo "9. 📁 Проверка структуры проекта:" && echo "   Директории агентов:" && for agent in "watcher-agent" "executor-agent" "solver-agent"; do if [ -d "$agent" ]; then echo "   ✅ $agent: существует ($(find "$agent" -type f 2>/dev/null | wc -l) файлов)" else echo "   ⚠️  $agent: отсутствует"; fi; done && echo "" && echo "10. 🔄 Проверка Git статуса (актуальность кода):" && if [ -d ".git" ]; then echo "   Git репозиторий:" && echo "   Ветка: $(git branch --show-current 2>/dev/null || echo 'не определена')" && echo "   Последний коммит: $(git log -1 --format='%h - %s' 2>/dev/null || echo 'не доступен')" && UNTRACKED=$(git status --porcelain 2>/dev/null | wc -l) && if [ "$UNTRACKED" -gt 0 ]; then echo "   ⚠️  Неотслеживаемых изменений: $UNTRACKED"; else echo "   ✅ Нет неотслеживаемых изменений"; fi else echo "   ℹ️  Git репозиторий не инициализирован в этой директории"; fi && echo "" && echo "==================================================================" && echo "ЭТАП 4/4: ПЕРЕЗАПУСК И ФИНАЛЬНАЯ ПРОВЕРКА" && echo "==================================================================" && echo "" && echo "11. 🔄 Перезапуск всех сервисов (рекомендуется после восстановления):" && read -p "   Выполнить перезапуск Docker-контейнеров? (y/N): " -n 1 -r RESTART_REPLY && echo "" && if [[ $RESTART_REPLY =~ ^[Yy]$ ]]; then echo "   Останавливаем контейнеры..." && docker-compose down 2>/dev/null && sleep 3 && echo "   Запускаем контейнеры..." && docker-compose up -d 2>/dev/null && echo "   ✅ Сервисы перезапущены" && echo "   Ожидаем полный запуск (30 секунд)..." && for i in {1..6}; do echo -n "." && sleep 5; done && echo "" else echo "   ⏭️  Перезапуск пропущен по вашему выбору"; fi && echo "" && echo "12. 🎯 Финальная проверка работоспособности агентов:" && echo "   Система 'Триада Агентов':" && FINAL_CHECKS=0 && for check in "curl -s http://localhost:9091 >/dev/null" "curl -s http://localhost:8000/health >/dev/null" "curl -s http://localhost:8080 >/dev/null 2>&1"; do if eval timeout 3 $check 2>/dev/null; then FINAL_CHECKS=$((FINAL_CHECKS+1)); fi; done && if [ $FINAL_CHECKS -eq 3 ]; then echo "   ✅ ВСЕ 3 агента работают!"; elif [ $FINAL_CHECKS -eq 2 ]; then echo "   ⚠️  2 из 3 агентов работают"; elif [ $FINAL_CHECKS -eq 1 ]; then echo "   ⚠️  1 из 3 агентов работает"; else echo "   ❌ Ни один агент не отвечает"; fi && echo "" && echo "==========================================================" && echo "📋 ИТОГОВЫЙ ОТЧЕТ" && echo "==========================================================" && echo "" && echo "✅ Проверки выполнены: 12 основных тестов" && echo "⏱️  Время выполнения: $(date)" && echo "" && echo "🎯 РЕКОМЕНДАЦИИ:" && echo "1. Если все тесты пройдены ✅ - система готова к работе" && echo "2. Если есть предупреждения ⚠️  - проверьте соответствующие сервисы" && echo "3. Если есть ошибки ❌ - требуется диагностика проблемных компонентов" && echo "" && echo "🚨 КРИТИЧЕСКИ ВАЖНО ДАЛЕЕ:" && echo "1. Создать новую точку восстановления:" && echo "   cd ~/autoshop && ./create_restore_point.sh \"Система_восстановлена_$(date +%Y%m%d_%H%M)\"" && echo "2. Проверить логи на ошибки:" && echo "   docker-compose logs --tail=50 2>/dev/null | grep -i \"error\\|exception\\|failed\"" && echo "3. Обновить документацию о восстановлении" && echo "" && echo "==========================================================" && echo "🔥 СИСТЕМА ПРОВЕРЕНА. МОЖНО ПРИСТУПАТЬ К РАБОТЕ!" && echo "=========================================================="
```
## 🎯 Как использовать этот скрипт:
1.**Восстановите систему из точки восстановления**:

```bash
cd ~/autoshop && sudo ./restore_system.sh backups/2026-01-19_Redis_очередь_устраняет_пробки_у_контейнера
```

2. **Запустите скрипт проверки (команда выше)**

3. **Следуйте рекомендациям в итоговом отчёте**

## 📊 Что проверяет скрипт:

Этап	Что проверяется	Критерий успеха
1. Базовая проверка	Docker, порты, контейнеры	Все основные порты открыты
2. Функциональное тестирование	Prometheus, Executor, Redis, PostgreSQL	Сервисы отвечают на запросы
3. Проверка конфигураций	Файлы конфигурации, структура проекта	Ключевые файлы существуют
4. Финальная проверка	Перезапуск и проверка агентов	Все 3 агента системы работают

## ⏱️ Время выполнения: 5-10 минут

**После успешного прохождения всех проверок система будет гарантированно готова к работе. Скрипт также предложит создать новую точку восстановления уже из восстановленной системы**.

--- ФАЙЛ: RESTORE_SYSTEM.md ---
